namespace ManagementTypes {
	export interface ManagementData {
		openExportDialog: boolean; // Whether the export dialog is open
		leftToolboxOpen: boolean; // Whether the left toolbox is open
		rightToolboxOpen: boolean; // Whether the right toolbox is open
	}
}